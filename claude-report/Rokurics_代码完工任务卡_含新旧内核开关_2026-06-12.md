# Rokurics 代码完工任务卡 · 含"新/旧内核切换开关"

- 时间：2026-06-12 14:30 (CST)
- 目标：把代码**全部建完**,使你在设备上**一个开关即可在新/旧内核间切换**;之后由你实机测试。
- 验收口径：**全部为编译期/接线级**(构建零错误 + 文件/接线到位),**不依赖真机**——真机验证是你拿到 build 之后的下一步。
- 边界(Path B,务必告诉 Codex)：**不重写传输层**(TLS/HMAC/配对保留 legacy);音频走已建的 cutover 执行器(它已能驱动真实 `SecureMacUploadClient`);**只做接线,不再加新脚手架类型**;默认 `oldKernel` 安全。

---

## 现状关键点(任务卡的依据)
- 主开关 `CanonicalKernelSwitchEffectiveConfiguration` **已同时携带** `syncRuntimeConfiguration / applyRuntimeConfiguration / readRuntimeConfiguration`(:418-422)——开关本就设计成"决策+应用+读"一起切。
- **缺口①**:协调器只消费了 sync/apply,**没把 `readRuntimeConfiguration` 接到 `StudyLibraryStore`**(读仍靠独立 override)。
- **缺口②**:RealApplyPort / existence port / cutover 执行器在 app 构造点仍是 **nil / dry-run 默认**,没挂到开关。
- **缺口③**:卡顿(builder 仍 `struct`,`makeSyncManifest` 仍主线程 :483/:1972)、切回证明(0 处 app 调用)未做。

---

## 任务卡(按顺序,一次一卡)

### T1 · 卡顿收尾(独立,先做)
- **文件**：`Rokurics/StudyLibrarySyncCoordinator.swift`
- **结构**：`LocalNetworkSyncInventoryBuilder`(:1599)`struct`→`actor`,或把采集塞进已有 `nonisolated` 后台结构(`LocalNetworkSyncInventoryBackgroundIO/…BackgroundStudyManifestBuilder`)。
- **函数**：`buildRuntimeSnapshot(...)` 内 `loadAllMetadata`/`loadJobs`/目录扫描/`metadataHash`/`sha256` 全后台;`makeSyncManifest` 两处主线程调用(:483/:1972)改为"后台取快照 → 纯函数构建"。
- **验收(编译级)**：构建零错误;builder 不再 `@MainActor`;`buildRuntimeSnapshot` 内无对 `@MainActor` store 的直接重活调用。(durationMs 留你真机量)
- **依赖**：无

### T2 · 主开关驱动"读"(核心接线 —— 让一个开关真能切读)
- **文件**：`Rokurics/StudyLibrarySyncCoordinator.swift` + Mac 对应 + 双端 `StudyLibraryStore.swift`
- **结构**：无新类型
- **函数**：在 `refreshCanonicalKernelSwitchConfiguration` 里,除已有 sync/apply 外,**新增**把 `result.effectiveConfiguration.readRuntimeConfiguration` 传给 `studyLibraryStore.setCanonicalReadRuntimeConfiguration(...)`(已有入口:Mac :273 / iPhone :257);`oldKernel` 模式传 `nil`(回 legacy)。
- **验收(编译级)**：构建零错误;切档时 `canonicalReadRuntimeConfigurationOverride` 随之变化;`oldKernel→nil`(读 legacy)、`canonicalFullSync→`canonical 读配置。
- **依赖**：无

### T3 · 录音 ReadSeam 接进读路径
- **文件**：双端 `CanonicalReadRuntimeAdapter` / `StudyLibraryStore`
- **函数**：让读路径在录音域调用 `IPhone/MacRecordingMetadataReadSideSeam` 的 `evaluate/readSource`,与其它域一致(目前该 seam 仅定义、未被引用)。
- **验收(编译级)**：构建零错误;录音 ReadSeam 被运行时引用(非仅定义)。
- **依赖**：T2

### T4 · 执行器/端口注入生产路径并挂到开关(让 apply 真能切)
- **文件**：构造点 `RokuricsMac/RokuricsMacApp.swift`(:44/56)、`Rokurics/MacConnectionView.swift`(:111)、`RokuricsMac/SecureLocalHTTPSServer.swift`(existence port :1259)、iPhone 对应构造点
- **结构**：无新类型
- **函数**：按开关 `mode` 注入——
  - 录音域 `IPhone/MacRecordingMetadataRealApplyPort`(`canonicalFullSync`+owner-approved 用 production-root init;否则 dry-run/test);
  - `canonicalRecordingExistenceApplyPort`(漏传桥)由 `nil`→按 mode 注入;
  - 音频 `IPhone/MacAudioUploadCutoverExecutor` 按 mode 接进上传协调;
  - 库/产物/墓碑域 cutover 执行器确认已按 mode 接线。
- **验收(编译级)**：构建零错误;`canonicalFullSync` 路径能构造出 production 端口(仍受 blocker 守门);`oldKernel` 路径全部回 legacy/nil。
- **依赖**：T2

### T5 · `canonicalFullSync` 解锁生产根写(behind 现有 blocker)
- **文件**：`CanonicalKernelSwitch.swift` effectiveConfiguration builder + 构造点
- **函数**：当 `mode=canonicalFullSync` 且 `ownerApproved`+`manualConfirmation` 满足,让 RealApplyPort 以 `allowProductionRootWrites:true` 构造;否则保持 dry-run。**保留** `canonicalWritesNotLegacyReadable`/`legacyFallbackUnavailable`/`…RequiresOwnerApproval` 等 blocker。
- **验收(编译级)**：构建零错误;未确认时仍 dry-run;确认后才 production-root;blocker 生效。
- **依赖**：T4

### T6 · 切回证明 driver(命门 —— 代码侧接成可一键跑)
- **文件**：新增 Debug 入口(`IPhone/MacSettingsView` 的 `#if DEBUG` 区)+ 薄 driver
- **函数**：调 `CanonicalSwitchBackRealisticRootHarness.run(...)` 指向"真实库**副本**目录",产出 `CanonicalLegacySwitchBackProofResult` 落 jsonl。
- **验收(编译级)**：构建零错误;设置里有一个可点的"切回证明"按钮。(你上机点它,在副本上验证数据无损)
- **依赖**：无(可并行)

### T7 · 单一主开关 UI 收口
- **文件**：`Rokurics/IPhoneSettingsView.swift` / `RokuricsMac/MacSettingsView.swift`
- **函数**：一个用户可见的"内核模式"选择器(`oldKernel…canonicalFullSync`)驱动 `CanonicalKernelSwitch`,带已有确认弹窗;收敛/隐藏其余分散旋钮。
- **验收(编译级)**：构建零错误;设置里**一个开关**即可切 5 档;默认 `oldKernel`。
- **依赖**：T2、T4

---

## "代码完工"验收门(全绿 = build 完成,可上机)

| # | 门槛 | 判定 |
| --- | --- | --- |
| 1 | 构建零错误、现有测试全过 | 编译 |
| 2 | 开关默认 `oldKernel`;5 档可选 | 接线 |
| 3 | 切到 `canonicalFullSync`(+确认)后,**决策/读/apply(元数据+文件)/音频 commit 四条都由开关切到 canonical**;切回 `oldKernel` 全部回 legacy | 接线 |
| 4 | legacy 路径与兜底完整保留(不删) | 接线 |
| 5 | 切回证明 driver 可一键调用 | 接线 |
| 6 | Path B:传输/上传保留 legacy,未重写 | 边界 |

**给 Codex 的总约束(贴在最前)**：Path B,不重写 TLS/HMAC 传输;一次一卡,每卡只交"构建零错误 + 对应文件/接线",**不再加新脚手架类型**;默认 `oldKernel` 安全;每卡报告"改了哪些文件 + 是否构建通过"。

---

## 顺序与你接手的点
1. **Codex 顺序**：T1(卡顿)→ T2(开关驱动读)→ T3(录音读接线)→ T4(端口注入)→ T5(生产根解锁)→ T6(切回 driver)→ T7(UI 收口)。T1/T6 可与主线并行。
2. **全部 6 门全绿后** = 代码完工。你再上机:设置里把开关从 `oldKernel` 逐档上推(`canonicalShadow→DecisionOnly→ApplyNoAudio→FullSync`),每档导 jsonl 看诊断,先在**备份/测试机**、先跑**切回证明**。
3. 我这边:你每报"某卡做完",我按上面验收门**只读核验文件/接线是否真到位**(防再次夸大),再放行下一卡。
